// The chat UI lives in the persistent AppShell (see layout.tsx) so an in-flight answer keeps
// streaming while the user switches between chats; routes only select which chat is shown.
export default function NewChatPage() {
  return null;
}
